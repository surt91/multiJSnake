# this is for heroku, for this program to fit into their server
# also, it is crucial for heroku to pass `-Djavacpp.platform=linux-x86_64`
# to maven to not include very large but unnecessary native libraries
# (this can be achieved by setting the `MAVEN_CUSTOM_OPTS` variable in heroku)

node_modules/